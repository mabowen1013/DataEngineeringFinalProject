# Author: Bowen Ma
# Secrets for the trade analysis pipeline
# NOTE: Replace values before deploying to cluster.
# In production, use sealed-secrets or external secret managers.
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-secrets
  namespace: trade-pipeline
type: Opaque
stringData:
  FINNHUB_API_KEY: "d7g1depr01qqb8ri0mv0d7g1depr01qqb8ri0mvg"
  POSTGRES_PASSWORD: "airflow"
  DB_PASSWORD: "pipeline"
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin"
